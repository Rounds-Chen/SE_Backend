<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rounds.experimentalteachingsystm.mapper.TeacherMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.rounds.experimentalteachingsystm.entity.TeacherEntity">
        <id column="teacher_id" property="teacherId" />
        <result column="teacher_name" property="teacherName" />
        <result column="mail_addr" property="mailAddr" />
        <result column="teacher_pwd" property="teacherPwd" />
        <result column="identity" property="identity" />
        <result column="authority" property="authority" />
        <result column="gender" property="gender" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        teacher_id, teacher_name, mail_addr, teacher_pwd, identity, authority, gender
    </sql>

</mapper>
